# GeoPresence
A database-free Next.js application that automatically extracts EXIF metadata from photos, resolves GPS locations using OpenStreetMap, and generates attendance-style Check-In/Check-Out reports with CSV, Excel, and JSON export support. Optimized for Vercel deployment.
